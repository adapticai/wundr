/**
 * Settings Loading State
 * @module app/(workspace)/[workspaceId]/settings/loading
 */
import { SettingsSkeleton } from '@/components/skeletons';

export default function SettingsLoading() {
  return <SettingsSkeleton />;
}
