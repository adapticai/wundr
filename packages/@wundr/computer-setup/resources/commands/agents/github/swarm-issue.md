$ARGUMENTS

Spawn the Swarm Issue specialist agent to handle this task.

Use the Agent tool with subagent_type="swarm-issue" to execute.

The agent specializes in: GitHub issue-based swarm coordination agent that transforms issues into
intelligent multi-agent tasks with automatic decomposition and progress tracking

Task to perform: $ARGUMENTS
