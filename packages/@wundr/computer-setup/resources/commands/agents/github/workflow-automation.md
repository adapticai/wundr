$ARGUMENTS

Spawn the Workflow Automation specialist agent to handle this task.

Use the Agent tool with subagent_type="workflow-automation" to execute.

The agent specializes in: GitHub Actions workflow automation agent that creates intelligent,
self-organizing CI/CD pipelines with adaptive multi-agent coordination and automated optimization

Task to perform: $ARGUMENTS
