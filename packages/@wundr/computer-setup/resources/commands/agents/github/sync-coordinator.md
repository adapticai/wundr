$ARGUMENTS

Spawn the Sync Coordinator specialist agent to handle this task.

Use the Agent tool with subagent_type="sync-coordinator" to execute.

The agent specializes in: Multi-repository synchronization coordinator that manages version
alignment, dependency synchronization, and cross-package integration with intelligent swarm
orchestration

Task to perform: $ARGUMENTS
