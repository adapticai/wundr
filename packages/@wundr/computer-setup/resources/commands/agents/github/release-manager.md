$ARGUMENTS

Spawn the Release Manager specialist agent to handle this task.

Use the Agent tool with subagent_type="release-manager" to execute.

The agent specializes in: Automated release coordination and deployment with ruv-swarm orchestration
for seamless version management, testing, and deployment across multiple packages

Task to perform: $ARGUMENTS
