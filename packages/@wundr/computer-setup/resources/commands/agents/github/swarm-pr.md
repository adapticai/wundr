$ARGUMENTS

Spawn the Swarm PR specialist agent to handle this task.

Use the Agent tool with subagent_type="swarm-pr" to execute.

The agent specializes in: Pull request swarm management agent that coordinates multi-agent code
review, validation, and integration workflows with automated PR lifecycle management

Task to perform: $ARGUMENTS
