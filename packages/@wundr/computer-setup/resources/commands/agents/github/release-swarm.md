$ARGUMENTS

Spawn the Release Swarm specialist agent to handle this task.

Use the Agent tool with subagent_type="release-swarm" to execute.

The agent specializes in: Orchestrate complex software releases using AI swarms that handle
everything from changelog generation to multi-platform deployment

Task to perform: $ARGUMENTS
