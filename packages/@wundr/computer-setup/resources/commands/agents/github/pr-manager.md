$ARGUMENTS

Spawn the PR Manager specialist agent to handle this task.

Use the Agent tool with subagent_type="pr-manager" to execute.

The agent specializes in: Comprehensive pull request management with swarm coordination for
automated reviews, testing, and merge workflows

Task to perform: $ARGUMENTS
