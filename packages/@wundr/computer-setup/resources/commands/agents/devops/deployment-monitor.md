$ARGUMENTS

Spawn the Deployment Monitor specialist agent to handle this task.

Use the Agent tool with subagent_type="deployment-monitor" to execute.

The agent specializes in: Monitors deployment status on Railway/Netlify, detects failures, reports
progress

Task to perform: $ARGUMENTS
