$ARGUMENTS

Spawn the Log Analyzer specialist agent to handle this task.

Use the Agent tool with subagent_type="log-analyzer" to execute.

The agent specializes in: Deep analysis of build/runtime logs to identify root causes of deployment
failures

Task to perform: $ARGUMENTS
