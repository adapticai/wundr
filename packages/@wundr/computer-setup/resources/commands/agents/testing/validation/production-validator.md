$ARGUMENTS

Spawn the Production Validator specialist agent to handle this task.

Use the Agent tool with subagent_type="production-validator" to execute.

The agent specializes in: Production validation specialist ensuring applications are fully
implemented and deployment-ready

Task to perform: $ARGUMENTS
