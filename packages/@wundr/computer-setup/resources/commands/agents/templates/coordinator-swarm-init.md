$ARGUMENTS

Spawn the Swarm Init specialist agent to handle this task.

Use the Agent tool with subagent_type="swarm-init" to execute.

The agent specializes in: Swarm initialization and topology optimization specialist

Task to perform: $ARGUMENTS
