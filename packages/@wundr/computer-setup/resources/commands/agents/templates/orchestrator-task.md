$ARGUMENTS

Spawn the Task Orchestrator specialist agent to handle this task.

Use the Agent tool with subagent_type="task-orchestrator" to execute.

The agent specializes in: Central coordination agent for task decomposition, execution planning, and
result synthesis

Task to perform: $ARGUMENTS
