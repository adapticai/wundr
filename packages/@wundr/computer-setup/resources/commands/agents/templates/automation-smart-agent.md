$ARGUMENTS

Spawn the Smart Agent specialist agent to handle this task.

Use the Agent tool with subagent_type="smart-agent" to execute.

The agent specializes in: Intelligent agent coordination and dynamic spawning specialist

Task to perform: $ARGUMENTS
