$ARGUMENTS

Spawn the Performance Analyzer specialist agent to handle this task.

Use the Agent tool with subagent_type="perf-analyzer" to execute.

The agent specializes in: Performance bottleneck analyzer for identifying and resolving workflow
inefficiencies

Task to perform: $ARGUMENTS
