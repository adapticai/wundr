$ARGUMENTS

Spawn the Memory Coordinator specialist agent to handle this task.

Use the Agent tool with subagent_type="memory-coordinator" to execute.

The agent specializes in: Manage persistent memory across sessions and facilitate cross-agent memory
sharing

Task to perform: $ARGUMENTS
