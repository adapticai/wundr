$ARGUMENTS

Spawn the Hierarchical Coordinator specialist agent to handle this task.

Use the Agent tool with subagent_type="hierarchical-coordinator" to execute.

The agent specializes in: Queen-led hierarchical swarm coordination with specialized worker
delegation

Task to perform: $ARGUMENTS
