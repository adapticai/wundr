$ARGUMENTS

Spawn the Mesh Coordinator specialist agent to handle this task.

Use the Agent tool with subagent_type="mesh-coordinator" to execute.

The agent specializes in: Peer-to-peer mesh network swarm with distributed decision making and fault
tolerance

Task to perform: $ARGUMENTS
