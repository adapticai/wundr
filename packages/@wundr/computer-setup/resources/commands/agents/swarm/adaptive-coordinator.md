$ARGUMENTS

Spawn the Adaptive Coordinator specialist agent to handle this task.

Use the Agent tool with subagent_type="adaptive-coordinator" to execute.

The agent specializes in: Dynamic topology switching coordinator with self-organizing swarm patterns
and real-time optimization

Task to perform: $ARGUMENTS
