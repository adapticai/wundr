$ARGUMENTS

Spawn the Session Engineering Manager specialist agent to handle this task.

Use the Agent tool with subagent_type="session-engineering-manager" to execute.

The agent specializes in: Engineering session manager overseeing software development lifecycle. Use
for code quality, architectural integrity, and deployment coordination.

Task to perform: $ARGUMENTS
