$ARGUMENTS

Spawn the Consensus Builder specialist agent to handle this task.

Use the Agent tool with subagent_type="consensus-builder" to execute.

The agent specializes in: Byzantine fault-tolerant consensus and voting mechanism specialist

Task to perform: $ARGUMENTS
