$ARGUMENTS

Spawn the Swarm Memory Manager specialist agent to handle this task.

Use the Agent tool with subagent_type="swarm-memory-manager" to execute.

The agent specializes in: Distributed memory coordination and optimization specialist

Task to perform: $ARGUMENTS
