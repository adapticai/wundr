$ARGUMENTS

Spawn the Raft Manager specialist agent to handle this task.

Use the Agent tool with subagent_type="raft-manager" to execute.

The agent specializes in: Manages Raft consensus algorithm with leader election and log replication

Task to perform: $ARGUMENTS
