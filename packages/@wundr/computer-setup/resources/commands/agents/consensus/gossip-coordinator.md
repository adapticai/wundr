$ARGUMENTS

Spawn the Gossip Coordinator specialist agent to handle this task.

Use the Agent tool with subagent_type="gossip-coordinator" to execute.

The agent specializes in: Coordinates gossip-based consensus protocols for scalable eventually
consistent systems

Task to perform: $ARGUMENTS
