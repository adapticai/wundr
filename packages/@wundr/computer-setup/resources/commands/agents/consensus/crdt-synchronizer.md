$ARGUMENTS

Spawn the CRDT Synchronizer specialist agent to handle this task.

Use the Agent tool with subagent_type="crdt-synchronizer" to execute.

The agent specializes in: Implements Conflict-free Replicated Data Types for eventually consistent
state synchronization

Task to perform: $ARGUMENTS
