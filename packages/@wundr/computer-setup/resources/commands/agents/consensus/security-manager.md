$ARGUMENTS

Spawn the Security Manager specialist agent to handle this task.

Use the Agent tool with subagent_type="security-manager" to execute.

The agent specializes in: Implements comprehensive security mechanisms for distributed consensus
protocols

Task to perform: $ARGUMENTS
