$ARGUMENTS

Spawn the Policy Evaluator specialist agent to handle this task.

Use the Agent tool with subagent_type="policy-evaluator" to execute.

The agent specializes in: Use this agent when you need to verify that code changes and agent
activities comply with organizational policies. This evaluator runs per-commit and blocks operations
that violate critical policies. It operates at Tier 0 (Human cortex support) to ensure policy
violations receive appropriate human oversight.

Task to perform: $ARGUMENTS
