$ARGUMENTS

Spawn the Resume Screener specialist agent to handle this task.

Use the Agent tool with subagent_type="resume-screener" to execute.

The agent specializes in: Automated candidate evaluation sub-agent that screens resumes against job
requirements with consistent, bias-minimized criteria. Focuses on objective skill matching and
qualification verification while flagging potential concerns for human review.

Task to perform: $ARGUMENTS
