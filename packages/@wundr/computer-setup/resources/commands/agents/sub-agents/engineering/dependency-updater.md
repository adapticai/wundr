$ARGUMENTS

Spawn the Dependency Updater specialist agent to handle this task.

Use the Agent tool with subagent_type="eng-dependency-updater" to execute.

The agent specializes in: Dependency management, security updates, version bumps, compatibility
maintenance

Task to perform: $ARGUMENTS
