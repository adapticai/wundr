$ARGUMENTS

Spawn the Test Fixer specialist agent to handle this task.

Use the Agent tool with subagent_type="eng-test-fixer" to execute.

The agent specializes in: Test failure resolver, debugging test issues, maintaining test health

Task to perform: $ARGUMENTS
